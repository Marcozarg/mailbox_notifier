{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "agent",
            "if": "Bash(git push*)",
            "timeout": 90,
            "statusMessage": "Running code review before push...",
            "prompt": "A git push is about to happen. Review the changes before allowing it.\n\n1. Run `git diff origin/main...HEAD` to see all unpushed changes.\n2. Run `git log origin/main...HEAD --oneline` to see the commits.\n3. Check for these issues specific to this firmware project:\n   - FW_VERSION macro is defined exactly once and matches the version in the file header comment block\n   - Version bump follows the semver rules: patch for bug/typo, minor for feature/behaviour, major for hardware/packet-format break\n   - No bugs from the 'Notable bugs fixed' list in CLAUDE.md have been re-introduced\n   - MQTT topics only use the scheme defined in CLAUDE.md (no new top-level mailbox/<thing> topics)\n   - HA entity names do NOT include the mailbox_ prefix in name/unique_id/object_id fields\n   - If sender firmware changed, flag it explicitly (requires physical trip to mailbox)\n4. Check documentation consistency:\n   - CHANGELOG.md must have an entry whose version matches the current FW_VERSION in the .ino file. If the version exists in the .ino but not in CHANGELOG.md, block the push.\n   - If the diff modifies any publishOne() call (changing the retained argument), verify that README.md was also modified in this push to reflect the change in the MQTT topic table. If not, block and say which topics need updating.\n   - If the diff adds or removes publishOneDiscovery() calls, verify that README.md entity count and entity list were updated. If not, block and say what is missing.\n5. If you find real issues, output ONLY this JSON (no other text):\n{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"Code review blocked push: <concise summary of issues>\"}}\n6. If everything looks good, output ONLY this JSON:\n{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"allow\"}}"
          }
        ]
      }
    ]
  }
}
